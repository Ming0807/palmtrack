# PalmTrack decision comps

These image-generation outputs are review evidence for the 2026-08-25 field UI decision. They are not shipped UI assets and contain synthetic labels only. Each PNG embeds its full generation prompt through the Impeccable `impeccable:prompt` metadata field; the matching text file is retained for diffable provenance.

| Direction | Preview | Role in the decision |
|---|---|---|
| A — carbon-copy queue | [PNG](direction-a-queue.png) · [prompt](direction-a-queue.prompt.txt) | Selected structure for the collector home |
| B — weighbridge receipt | [PNG](direction-b-receipt.png) · [prompt](direction-b-receipt.prompt.txt) | Retain numeric alignment; reject dense home composition |
| C — field handoff route | [PNG](direction-c-route.png) · [prompt](direction-c-route.prompt.txt) | Selected structure inside one assignment |

The generated wording and fictional geography are visual references, not domain truth. Implementation copy and workflow order come from the accepted source-of-truth documents.
